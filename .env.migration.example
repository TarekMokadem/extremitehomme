# Copiez ce fichier en .env.migration et remplissez la valeur.
# Supabase Dashboard → Settings → Database → Connection string → URI (mode "Session" ou "Transaction")

SUPABASE_DB_URI="postgresql://postgres.[PROJECT_REF]:[MOT_DE_PASSE]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres"

# Ou en connexion directe (port 5432) :
# SUPABASE_DB_URI="postgresql://postgres:[MOT_DE_PASSE]@db.[PROJECT_REF].supabase.co:5432/postgres"
